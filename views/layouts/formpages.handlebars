<!DOCTYPE html>
<html lang="en">
<head>
    {{>head}}
    <title>Laura Martinez - Health Coach</title>
</head>
<body>
    {{>en/navigation}}
    <main>
        <div class="container z-depth-5">
            {{>en/formnav}}
            {{{body}}}
            {{>en/formfooter}}
        </div>
    </main>
    {{>en/footer}}

    <style type="text/css">
        body {
            display: flex;
            min-height: 100vh;
            flex-direction: column;
        }

        main {
            flex: 1 0 auto;
        }
    </style>

</body>
</html>